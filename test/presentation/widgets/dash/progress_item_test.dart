import 'package:flutter_test/flutter_test.dart';
import 'package:raja_c_flutter_biometrics/presentation/widgets/dash/progress_item.dart';

void main() {
  testWidgets('ProgressItem renders', (WidgetTester tester) async {
    // Provide required progressItem mock
    // await tester.pumpWidget(ProgressItem(progressItem: ...));
    // expect(find.byType(ProgressItem), findsOneWidget);
  });
}
