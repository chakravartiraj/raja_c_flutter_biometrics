import 'package:flutter_test/flutter_test.dart';
import 'package:raja_c_flutter_biometrics/presentation/widgets/dash/project_card.dart';

void main() {
  testWidgets('ProjectCard renders', (WidgetTester tester) async {
    // Provide required project mock
    // await tester.pumpWidget(ProjectCard(project: ...));
    // expect(find.byType(ProjectCard), findsOneWidget);
  });
}
