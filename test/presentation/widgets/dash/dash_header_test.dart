import 'package:flutter_test/flutter_test.dart';
import 'package:raja_c_flutter_biometrics/presentation/widgets/dash/dash_header.dart';

void main() {
  testWidgets('DashboardHeader renders', (WidgetTester tester) async {
    // Provide required userData mock
    // await tester.pumpWidget(DashboardHeader(userData: ...));
    // expect(find.byType(DashboardHeader), findsOneWidget);
  });
}
