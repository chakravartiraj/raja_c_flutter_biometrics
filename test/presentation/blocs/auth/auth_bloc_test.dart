import 'package:flutter_test/flutter_test.dart';
import 'package:raja_c_flutter_biometrics/presentation/blocs/auth/auth_bloc.dart';

void main() {
  test('AuthBloc can be instantiated', () {
    // Provide required biometricRepository mock
    // final bloc = AuthBloc(biometricRepository: ...);
    // expect(bloc, isA<AuthBloc>());
  });
}
